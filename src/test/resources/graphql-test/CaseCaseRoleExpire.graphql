mutation ExpireCaseRole( $caseId: ID!, $caseRoleId: ID!){
    expireCaseRole(caseId: $caseId, caseRoleId: $caseRoleId){
        id
        fromDate
        thruDate
        partyEntity {
            id
        }
        caseRoleType {
            id
            description
        }
    }
}
